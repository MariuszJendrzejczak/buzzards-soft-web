# Live session (Chrome) — scratch, overwrite-allowed
dev url: http://localhost:3001
note: root / is 404 by design (output:"export", no middleware redirect) — use /pl | /en | /sv
task: visual review + polish of the NEW mobile-apps offer subpage /mobile-apps-offer.
  Full draft already exists (slice client-mobile-offer): 11 sections on the SB7 spine mirroring
  the web offer + mobile-specific additions (examples, tier comparison, store-publishing honesty).
  Copy: mobileOffer namespace in messages/pl.json (PL only — en/sv NOT yet added). PL first.
route under review: /pl/mobile-apps-offer
mode: LIVE — chrome-devtools MCP tools callable this session (console/network/snapshot).
last action: fresh dev server on :3001, page renders full (all 11 section ids present).

sections (top->bottom): hero · audience · examples · guide · includes · pricing · comparison ·
  publishing · ownership · faq · quote.
components: components/sections/mobile-offer/*.tsx
copy: messages/pl.json -> mobileOffer.* (lines ~901-1159)

known gaps (by design, draft):
- mobileOffer namespace MISSING from en.json / sv.json -> /en /sv error + i18n-completeness test
  RED. PL is the working surface. Add EN/SV only when the draft is approved.

recent edits:
- COPY CONSOLIDATION sweep pass 1 (banked web-offer rules applied to mobileOffer.*):
  - NEGATION-CONTRAST (zero-tolerance) killed ×3: publishing.heading "bez niespodzianek"
    -> "czego się spodziewać"; guide.conversation "Rozmawiasz ze mną, nie z botem"
    -> "Rozmawiasz bezpośrednio ze mną"; ownership.limits.graphics "Jestem deweloperem,
    nie grafikiem" sentence dropped (lead already frames it).
  - OWNERSHIP echo "na własność" trimmed: meta.description tail dropped; audience.body1
    "oddany na własność" dropped. Ownership now lives in hero heading/subhead + ownership sec.
  - AI-solo pitch dedup: guide.build "każdą decyzję sprawdzam osobiście" tail dropped
    (accountability stays in audience.body3).
- STILL OPEN: audience.body2 = full re-pitch of hero (AI->faster->cheaper) + "na własność"
  + "dostępnej cenie" echoes -> needs creative rewrite (proposed 2 opts to user, not landed).
- STILL OPEN (structure): single-sentence card grids ("kafelki z jednym zdaniem") in
  Includes (7), Publishing (4), Examples (3) — user flagged the web anti-pattern is back.
- NEW SKILL built: ~/.claude/skills/copy-to-page-pl (orchestrator: generate -> gate ->
  land PL -> show live; PL only, never en/sv, never commits). Bundled scripts/land_copy.py
  (deep-merge into pl.json; round-trip identical; guards non-pl.json + out-of-namespace).
- SMOKE TEST PASSED end-to-end on mobileOffer.hero: copywriter agent generated
  hero-pl.md (VGT 12/12 MATCHED, killed the AI/ownership/price echoes) -> gate ->
  landing-mobileOffer.json -> land_copy.py -> pl.json -> rendered live on :3001, console
  clean. Hero now: heading "…opublikowana i w całości Twoja"; subheading de-crammed
  (3 one-job sentences). ctaSecondary "Zobacz, jak to działa" -> "Zobacz, jak pracuję".
  ⚠ EN/SV parity for mobileOffer.hero now owed (separate transcreate-copy-en step).
- FULL PAGE landed via copy-to-page-pl: all 10 remaining sections (audience, examples,
  guide, includes, pricing, comparison, publishing, ownership, faq, quote) generated by
  parallel copywriter agents (each VGT 12/12), whole-page consolidation applied 4 residual
  fixes (audience.body1 contrast+"nie prototyp"; publishing.twoTracks 2 negations;
  faq.subscription "nie masz abonamentu"; ownership/faq graphics-referral consistency),
  landed via land_copy.py (141 keys, 55 changed), rendered on :3001 console clean. Includes
  now meaty (fixed "kafelki z jednym zdaniem"). Artifacts: context/slices/client-mobile-offer/
  {audience,examples,guide,includes,pricing,comparison,publishing,ownership-mobile,faq,quote}-pl.md
  + landing-mobileOffer-full.json.
  ⚠ OPEN (owner decisions, facts-sheet ⚠ CONFIRM): promo date vs flat "cena startowa";
  prices 9900/19900 final?; store account default personal vs company; graphics referral.
  ⚠ EN/SV parity for ALL mobileOffer.* now owed (transcreate-copy-en) before build/PR.
- FLOW FIX (user: parallel blind agents cause cross-section repetition — must be ONE agent
  with whole context): copy-to-page-pl SKILL.md step 2 now forbids blind per-section
  parallelism (generate whole page in ONE context) + step 3 adds a mandatory data-driven
  whole-page repetition sweep. New bundled scripts/scan_repetition.py (cross-section n-gram
  scan) + land_copy.py --refresh-only guard (refuses non-existent paths → catches dropped
  '.items.'). Applied to this page: one whole-context copywriter agent deduped 18 keys ->
  dedup landing (+2 path-corrected patches). Result: "apple i google" 7x->4x, "równolegle
  z budową"/"wymogi Apple i Google"/"kod-projekt-chmura" cluster eliminated; remaining are
  inherent vocab (aplikacja/google/konta on a store-publishing offer) + FAQ restating facts
  + ~4 minor 2x phrases. Console clean.
- NEW memory banked (user): no-laconic-copy (Substance floor — every field 2-3 concrete
  sentences; "longer" = more substance). Honored in dedup (compressed echoes, kept substance).
